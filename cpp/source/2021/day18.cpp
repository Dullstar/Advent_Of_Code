#include <optional>
#include <chrono>
#include <string>
#include <iostream>
#include <fmt/format.h>
#include "../days.h"

std::optional<std::chrono::duration<float>> run_2021_day_18(const std::string& filename)
{
	// auto start_time = std::chrono::system_clock::now();
	// auto part1_start = std::chrono::system_clock::now();
	// auto part2_start = std::chrono::system_clock::now();
	// auto end_time = std::chrono::system_clock::now();

	// std::cout << "Elapsed Time:\n";
	// std::cout << fmt::format("Parsing: {} ms\n", (part1_start - start_time).count() * 1000);
	// std::cout << fmt::format("Part 1: {} ms\n", (part2_start - part1_start).count() * 1000);
	// std::cout << fmt::format("Part 2: {} ms\n", (end_time - part2_start).count() * 1000);
	// auto elapsed_time = (end_time - start_time);
	// std::cout << fmt::format("Total: {} ms\n", elapsed_time.count() * 1000);
	// return std::make_optional(elapsed_time);

	return {};
}